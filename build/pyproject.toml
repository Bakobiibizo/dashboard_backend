[tool.poetry]
name = "wallet"
version = "0.1.0"
description = "Chain agnostic wallet for web3 applications"
authors = ["Richard <bakobiibizo@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
communex = "^0.1.33.1"
loguru = "^0.7.2"
cryptography = "^43.0.0"
jinja2 = "^3.1.4"


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.2"
pytest-asyncio = "^0.23.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
